Copies of `../../shared_spectra/` — identical source data (this detector
preserves native image resolution at inference, no resize, so the shared
generator-level frequency analysis applies unchanged). See
`../../shared_spectra/README.md` for the full write-up and how to read
these numbers.
